import { render } from '@testing-library/react-native';
import React from 'react';

import { {{pascalCase componentName}} } from '#features/{{dashCase name}}/presentation/components';

describe('{{pascalCase componentName}} component', () => {
  it('should render provide text', async () => {
    // arrange
    const view = render(<{{pascalCase componentName}} text="{{pascalCase componentName}}" />);

    // assert
    expect(await view.findByText('{{pascalCase componentName}}')).toBeVisible();
  });
});
