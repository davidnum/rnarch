import { injectable } from 'inversify';
import { Either } from 'monet';

import { {{pascalCase name}}Dto } from '#features/{{dashCase name}}/data/dto';
import { HttpClient, HttpFailure } from '#infrastructure/http';

@injectable()
export class {{pascalCase name}}RemoteDataSource {
  constructor(private readonly httpClient: HttpClient) {}

  public async get{{pascalCase name}}(): Promise<Either<HttpFailure, {{pascalCase name}}Dto>> {
    const result = await this.httpClient.get<{{pascalCase name}}Dto>('/{{dashCase name}}');
    return result.map(response => response.data);
  }
}
